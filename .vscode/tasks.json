{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "st-stm32-ide-debut-launch.build",
            "type": "shell",
            "command": "cmake --build ${workspaceRoot}/build",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
